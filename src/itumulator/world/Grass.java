package itumulator.world;

import itumulator.simulator.Actor;

public class Grass implements Actor {
    @Override
    public void act(World world) {
        // Grass does not act, so this method can remain empty
    }
}
