package itumulator.world;

import itumulator.simulator.Actor;

public class Rabbit implements Actor {
    @Override
    public void act(World world) {
        // Rabbit does not act for now, so this method can remain empty
    }
}
